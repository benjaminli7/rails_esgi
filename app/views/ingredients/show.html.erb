<h1><%= @ingredient.name %></h1>

<ul>
    <li>
        <%= link_to "Edit", edit_ingredient_path(@ingredient) %>
    </li>
    <li>
        <%= link_to "Delete", ingredient_path(@ingredient), data: {
            method: :delete,
            confirm: "Are you sure?"
        } %>
    </li>
</ul>

<%= link_to "/ingredients", ingredients_path %>
